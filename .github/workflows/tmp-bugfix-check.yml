name: MatchApp bugfix check

on:
  push:
    branches:
      - matchapp-bugfix-check

permissions:
  contents: write

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: matchapp-bugfix-check
      - run: echo "workflow ok"
