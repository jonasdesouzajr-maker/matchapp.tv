name: Validate website
on:
  push:
    branches: [main, staging/media-enrichment-20260917]
  pull_request:
permissions:
  contents: read
concurrency:
  group: validate-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: npm
      - run: npm ci --ignore-scripts
      - run: node --check tools/scrape-catalog.js
      - run: node --check media-enrichment.js
      - run: npm test
      - run: npm run audit:site
