/* Mirrors useful activity categories to the signed-in account without uploading prompts, payment data, tokens or arbitrary metadata. */
(function(){'use strict';function install(){const a=window.MatchActivity;if(!a||a.__cloudBridge||typeof a.log!=='function')return;const original=a.log.bind(a),labels={match:'Matched a title',ai:'Used Ask AI',save:'Saved a title',remove:'Removed a title',seen:'Marked a title as seen',share:'Shared a Match',purchase:'Completed a purchase action',platform:'Opened a viewing service',together:'Used Match Together',settings:'Changed a setting'};a.log=function(type,detail,meta){original(type,detail,meta);const sb=window.supabaseClient;if(!sb||!labels[type])return;const safeData={};if(type==='match'&&meta?.platform)safeData.platform=String(meta.platform).slice(0,60);sb.rpc('record_user_activity',{p_type:type,p_label:labels[type],p_data:safeData}).then(()=>document.dispatchEvent(new CustomEvent('matchapp:historychange'))).catch(()=>{});};a.__cloudBridge=true;}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',install);else install();setTimeout(install,0);})();
